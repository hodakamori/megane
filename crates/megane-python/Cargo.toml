[package]
name = "megane-python"
version = "0.3.1"
edition = "2021"

[lib]
name = "megane_parser"
crate-type = ["cdylib"]

[dependencies]
megane-core = { path = "../megane-core" }
pyo3 = { version = "0.24", features = ["extension-module"] }
numpy = "0.24"
