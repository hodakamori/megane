[package]
name = "megane-wasm"
version = "0.2.0"
edition = "2021"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
megane-core = { path = "../megane-core" }
wasm-bindgen = "0.2"
js-sys = "0.3"
